syntax = "proto3";
package proto;
option go_package = "./proto";

message DownloadReq {
  string id = 1;
  string name = 2;
  string version = 3;
  string bucket = 4;
}

message Empty {}

service Shadow {
  rpc Download(DownloadReq) returns (Empty);
}
