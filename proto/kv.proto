syntax = "proto3";
package proto;
option go_package = "./proto";

message PutRequest {
  string key = 1;
  bytes value = 2;
}

message Empty {}

service Shadow {
  rpc Download(PutRequest) returns (Empty);
}
